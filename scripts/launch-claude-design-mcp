#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
plugin_root=$(CDPATH= cd -- "$script_dir/.." && pwd)

exec /usr/bin/env node \
  "$plugin_root/vendor/claude-design-mcp/bin/claude-design-mcp.mjs" "$@"
